namespace Sandbox2;

class Program {

    static void Main(string[] args) {
        Console.WriteLine("Howdy!");
        // Throw 3 dice
        Random randomGenorator = new();
        int d1 = randomGenorator.Next(1, 7);
        int d1 = randomGenorator.Next(1, 7);
        int d1 = randomGenorator.Next(1, 7);

        if (d2 < d3) {
            (d2. d3) = (d3, d2);
        }
        if (d2 < d3) {
            (d2. d3) = (d3, d2);
        }
    }
}
